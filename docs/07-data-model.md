# Data Model

## Purpose

This document describes the initial application data model assumptions for AdventureWorksAIWorkspace.

This is not an implementation document yet. It should guide future database and domain modeling.

## Core Entities

## User

Represents an authenticated application user.

Potential fields:

- Id
- Email
- DisplayName
- CreatedAt
- UpdatedAt

## Report

Represents a saved AI-generated business report.

Potential fields:

- Id
- UserId
- Title
- OriginalPrompt
- Summary
- Status
- IsFavorite
- CreatedAt
- UpdatedAt

## ReportConversation

Represents a conversation attached to a report.

Potential fields:

- Id
- ReportId
- CreatedAt
- UpdatedAt

## ReportMessage

Represents a single message in a report conversation.

Potential fields:

- Id
- ConversationId
- Role
- Content
- CreatedAt

Possible roles:

- User
- Assistant
- System

## GeneratedSqlQuery

Represents SQL generated by AI or reused by the system.

Potential fields:

- Id
- ReportId
- UserPrompt
- SqlText
- Explanation
- ValidationStatus
- ExecutionStatus
- CreatedAt

## ChartDefinition

Represents a chart or visualization configuration attached to a report.

Potential fields:

- Id
- ReportId
- ChartType
- Title
- Description
- DataSourceQueryId
- ConfigurationJson
- SortOrder

Possible chart types:

- Bar
- Line
- Pie
- Table
- KPI
- Area

## ReportTag

Represents a tag assigned to a report.

Potential fields:

- Id
- UserId
- Name
- CreatedAt

## ReportTagAssignment

Represents the many-to-many relationship between reports and tags.

Potential fields:

- ReportId
- TagId

## ExportHistory

Represents an export action for a report.

Potential fields:

- Id
- ReportId
- ExportType
- FileName
- CreatedAt

Possible export types:

- PDF
- Excel

## Open Data Model Questions

- Should raw query result data be stored, or should reports store only SQL and chart configuration?
- Should generated SQL be versioned per report refinement?
- Should chart configuration be stored as JSON?
- Should report conversations be modeled as separate conversations or directly as report messages?
- Should users be able to share reports with other users in the future?
